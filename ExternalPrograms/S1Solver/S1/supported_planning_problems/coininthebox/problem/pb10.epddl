(define (problem pb10)
  (:domain coininthebox)
  (:agents a b)
  (:depth 3)
  (:init (tail) (has_key a) (looking a) ([a b](has_key a)) ([a b](not (has_key b))) ([a b](not (opened))) ([a b](looking a)) ([a b](not (looking b))))
  (:goal ([a][b](tail)))
)