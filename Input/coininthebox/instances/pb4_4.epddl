(define (problem pb4_4)
  (:domain coininthebox)
  (:agents a b)
  (:depth 3)
  (:dynck false)
  (:init (tail) (has_key b) (looking a) ([a b](has_key b)) ([a b](not (has_key a))) ([a b](not (opened))) ([a b](looking a)) ([a b](not (looking b))))
  (:goal ([b](tail)))
)